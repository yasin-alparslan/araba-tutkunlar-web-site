import {redirect} from 'next/navigation'; import {getSession} from '@/lib/auth';
export default async function Account(){const s=await getSession(); if(!s) redirect('/login'); return <main className="min-h-screen bg-[#0b1318] text-white p-8"><div className="max-w-2xl mx-auto bg-[#142129] rounded-2xl p-8"><h1 className="text-3xl font-bold mb-6">Hesabım</h1><p className="mb-3"><span className="text-gray-400">Kullanıcı adı:</span> {s.username}</p><p className="mb-3"><span className="text-gray-400">E-posta:</span> {s.email}</p><p><span className="text-gray-400">Rol:</span> {s.role}</p></div></main>}
