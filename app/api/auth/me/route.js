import {NextResponse} from 'next/server'; import {getSession} from '@/lib/auth'; export async function GET(){const s=await getSession(); return s?NextResponse.json({user:s}):NextResponse.json({user:null},{status:401});}
